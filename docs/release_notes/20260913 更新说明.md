20260913 更新说明（20260909 至 20260913）

更新重点：
- 修复 DFLash2 等情况涉及的 torch_xcpu aoti generator 的问题
- 重构了 sparse indexer 和 sparse mla，使之算子结构更规范化，新增了复杂多后端算子的标准基类。
- 修复了 DFlash2 在 vLLM 中有不经过 torch_xcpu 调用 Linear 的问题
- 新增了 DFlash2 涉及的矩阵乘法的测试用例

## `torch_xcpu` 算子变更

统计范围为 2026-09-09（不含）至 `c423be935a87a0a581ccad3357ab0b605fbe6096`（含）。按本次发布要求，略过 sparse indexer 和 sparse MLA 相关算子。以下测试用于佐证所述行为，不代表覆盖全部 shape、dtype、后端和并行组合。

- `dflash2_grouped_conv`（小修改）：PrivateUse1 dispatcher 改用无歧义 `TORCH_FN` 入口注册，并显式加入 AOTI wrapper 生成头文件；算子 schema 和计算行为不变。
	- commit `a456c2b5213a3edd63ba16ed3bc083259e2e3e11`：[Compile] update some kernels to fix aoti wrappers
	- 测试：`torch_xcpu/test/ops/custom_ops/test_dflash2_grouped_conv.py`
- `einsum_blpr_blcr_to_blpc`（小修改）：PrivateUse1 dispatcher 改用无歧义 `TORCH_FN` 入口注册，并显式加入 AOTI wrapper 生成头文件；算子 schema 和计算行为不变。
	- commit `a456c2b5213a3edd63ba16ed3bc083259e2e3e11`：[Compile] update some kernels to fix aoti wrappers
	- 测试：`torch_xcpu/test/ops/custom_ops/test_einsum_blpr_blcr_to_blpc.py`
- `apply_top_k_top_p`（小修改）：将 CPU / PrivateUse1 dispatcher 注册拆分到独立 C++ 翻译单元，增加无歧义入口并加入 AOTI wrapper 生成；现有 schema 和计算行为不变。
	- commit `a456c2b5213a3edd63ba16ed3bc083259e2e3e11`：[Compile] update some kernels to fix aoti wrappers
	- 测试：`torch_xcpu/test/ops/custom_ops/test_topk_topp_sampler.py`

---

- vllm
	- 01219a28b 2026-09-13 kunzh [Fixup] set is_prefilling=torch.zeros in speculator.py（修改 1 个文件，新增 4 行，删除 0 行）
	- e3c5afd7f 2026-09-13 kunzh [Dflash] use torch_xcpu.ops.linear in DFlashQwen3Model（修改 1 个文件，新增 11 行，删除 4 行）

- torch_mcpu
	- 无

- torch_xcpu
	- c423be9 2026-09-13 kunzh [DSA][Refactor] refactor dsa impl in deepseek_v32（修改 50 个文件，新增 2353 行，删除 1449 行）
	- 484dbfe 2026-09-13 kunzh [Test][DFlash] update dflash and dflash2 mm cases（修改 2 个文件，新增 56 行，删除 1 行）
	- a456c2b 2026-09-12 kunzh [Compile] update some kernels to fix aoti wrappers（修改 24 个文件，新增 264 行，删除 146 行）
	- a78b1d9 2026-09-11 kunzh [DSA] re-impl optimized sparse_mla_fp8（修改 17 个文件，新增 2046 行，删除 107 行）

- torch_mpi_ext
	- 无

- vllm-xcpu-plugin
	- 0f2f45a 2026-09-13 kunzh [DSA] update get_paged_mqa_logits_metadata_shape（修改 1 个文件，新增 6 行，删除 3 行）
	- f0afef5 2026-09-11 kunzh [DSA] update flashattn_mla_sparse_patch to use new sparse_mla_impl（修改 1 个文件，新增 116 行，删除 120 行）

- vllm-xcpu-dev-kit
	- fd43e85 2026-09-11 kunzh feat(release): update release publish scripts（修改 2 个文件，新增 64 行，删除 13 行）
	- e1d8a53 2026-09-09 kunzh fix(presets): fix _VLLM_OPTIONAL_ARGS= in serial/GLM-5.2 presets（修改 2 个文件，新增 0 行，删除 6 行）
	- 8574e28 2026-09-09 kunzh release: 20260909（修改 2 个文件，新增 95 行，删除 7 行）
